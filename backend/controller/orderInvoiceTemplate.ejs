<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Invoice</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
        }
        .container {
            max-width: 800px;
            margin: 30px auto;
            background: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            color: #333333;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #4CAF50;
            padding-bottom: 10px;
        }
        .header h1 {
            margin: 0;
            color: #4CAF50;
        }
        .header p {
            margin: 5px 0;
            color: #555555;
        }
        .section {
            margin: 20px 0;
        }
        .section h2 {
            margin-bottom: 10px;
            color: #333333;
            font-size: 20px;
            border-bottom: 1px solid #cccccc;
            padding-bottom: 5px;
        }
        .section p {
            margin: 5px 0;
            color: #555555;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        table thead tr {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
        }
        table th, table td {
            text-align: left;
            padding: 10px;
            border: 1px solid #dddddd;
        }
        table tbody tr:nth-child(odd) {
            background-color: #f9f9f9;
        }
        table tbody tr:nth-child(even) {
            background-color: #ffffff;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #777777;
        }
        .download-button {
            text-align: center;
            margin: 20px 0;
        }
        .download-button a {
            text-decoration: none;
            color: white;
            background-color: #4CAF50;
            padding: 12px 25px;
            border-radius: 5px;
            font-weight: bold;
            font-size: 16px;
        }
        .download-button a:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>BuildPro OGCS</h1>
            <p>Order Invoice</p>
        </div>

        <div class="section">
            <h2>Order Details</h2>
            <p><strong>Order ID:</strong> <%= orderId %></p>
            <p><strong>Total Amount:</strong> ₹<%= totalAmount.toFixed(2) %></p>
            <p><strong>GST:</strong> ₹<%= gst.toFixed(2) %></p>
            <p><strong>Delivery Charges:</strong> ₹<%= deliveryCharges.toFixed(2) %></p>
            <p><strong>Status:</strong> <%= status %></p>
        </div>

        <div class="section">
            <h2>Delivery Address</h2>
            <p><strong>House/Apartment:</strong> <%= address.houseNumberOrApartment %></p>
            <p><strong>Street/Area:</strong> <%= address.areaOrStreet %></p>
            <p><strong>Landmark:</strong> <%= address.landmark %></p>
            <p><strong>City:</strong> <%= address.cityOrTown %></p>
            <p><strong>State:</strong> <%= address.selectedState %></p>
            <p><strong>Postal Code:</strong> <%= address.postalCode %></p>
        </div>

        <div class="section">
            <h2>Seller Details</h2>
            <% sellers.forEach((seller) => { %>
                <p><strong>Seller Name:</strong> <%= seller.name %></p>
                <p><strong>Email:</strong> <%= seller.email %></p>
                <p><strong>Contact:</strong> <%= seller.contact %></p>
            <% }); %>
        </div>

        <div class="section">
            <h2>User Details</h2>
            <p><strong>Name:</strong> <%= user.name %></p>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Mobile:</strong> <%= user.mobile %></p>
        </div>

        <div class="section">
            <h2>Items</h2>
            <table>
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>HSN Code</th>
                        <th>Quantity</th>
                        <th>Price</th>
                        <th>GST</th>
                        <th>Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    <% items.forEach((item) => { %>
                        <tr>
                            <td><%= item.name %></td>
                            <td><%= item.hsnCode %></td>
                            <td><%= item.quantity %></td>
                            <td>₹<%= item.price.toFixed(2) %></td>
                            <td>₹<%= item.gst.toFixed(2) %></td>
                            <td>₹<%= item.subtotal.toFixed(2) %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <div class="download-button">
            <a href="cid:invoice_<%= orderId %>" download="Invoice_<%= orderId %>.pdf">
                Download Invoice
            </a>
        </div>

        <div class="footer">
            <p>Thank you for shopping with BuildPro OGCS!</p>
            <p>If you have any questions or need assistance, please contact our support team.</p>
        </div>
    </div>
</body>
</html>
